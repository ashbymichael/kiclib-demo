<table class="pure-table pure-table-striped">
  <thead>
    <th>ID</th>
    <th>Title</th>
  </thead>
  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.id %></td>
        <td><%= book.title %></td>
      </tr>
    <% end %>
  </tbody>
</table>
